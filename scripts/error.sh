#!/usr/bin/env bash

# This script intentionally errors out during execution to simulate the behavior of a real script that encounters a runtime error
# This is useful for verifying the error handling behavior of the tool
echo "error.sh intentionally exiting with 1 status"
exit 1
